import React from "react";
import { Graph } from "../components/Graph";

export default function Main() {
  return (
    <Graph isWidget={false} openModal={() => {}} setSelectedNode={() => {}} />
  );
}
